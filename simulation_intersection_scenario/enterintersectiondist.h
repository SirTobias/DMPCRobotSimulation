#ifndef ENTERINTERSECTIONDIST_H
#define ENTERINTERSECTIONDIST_H

#include "arrivalcar.h"

#include <random>



/**
 * @brief The EnterIntersectionDist class creates a uniform distribution to obtain a equal distributed selection of entry points
 */
/*class EnterIntersectionDist
{
public:
    EnterIntersectionDist(const unsigned int &numberEntryPoints, const DistributionFunction &distrib, const double &meanArrivalTime, const TimeMetric &metric, const double &T);
    void setNumberOfEntryPoints(const unsigned int &number);
    unsigned int getNewRandomEntryPoint();
private:
    unsigned int m_numberEntryPoints;
    std::vector<ArrivalCar> m_arrivalCarProcess;
};
*/
#endif // ENTERINTERSECTIONDIST_H
